# .gitignore

# ------------------------------------------------------------------------------
# System & IDE
# ------------------------------------------------------------------------------
.DS_Store
.idea/
.vscode/

# Temporary files created by editors
*.swp
*.log

# ------------------------------------------------------------------------------
# Binary Files (Compiled files & Libraries)
# ------------------------------------------------------------------------------
# Ignore all jar, zip, tar.gz files
*.jar
*.zip
*.tar.gz

# But keep the library folder structure itself (with .gitkeep files)
!/libraries/**/.gitkeep

# ------------------------------------------------------------------------------
# Runtime Data
# ------------------------------------------------------------------------------
# Log and cache files
logs/
cache/
debug/
crash-reports/
.paper-remapped/

# Minecraft specific runtime files
ops.json
banned*.json
whitelist.json
usercache.json
version_history.json
permissions.yml
help.yml
wepif.yml

# Update date files
.version
.license
lastupdate
status.json
server.json
*.sqlite

# Backups folder created by 'everest backup' command
/backups/

# ------------------------------------------------------------------------------
# Server Instances (Core Data Management)
# ------------------------------------------------------------------------------
# Map data (very large, recommended to exclude)
*.dat_old
*.mcmeta
*.lock
*.mca
*.dat

# Ignore plugin symbolic links or JAR files but track configuration folders
servers/**/plugins/*.jar

# Ignore spark plugin data
servers/**/plugins/spark/

# Statistics and player data (optional: exclude if frequent resets)
servers/**/stats/
servers/**/playerdata/
servers/**/advancements/
servers/**/mcmmo_regions/

# Unnecessary files related to Paper/Spigot
servers/**/cache
servers/**/versions
servers/**/libraries
servers/**/.console_history

# Velocity language files (can be large and are not usually modified)
servers/velocity/lang

# GeyserMC locale files (can be large and are not usually modified)
servers/velocity/plugins/Geyser*/locales/*.json

# LuckPerms locale files and symlink files
servers/**/plugins/luckperms/translations
servers/**/plugins/luckperms/libs

# FastAsyncWorldEdit data files
servers/**/plugins/FastAsyncWorldEdit/schematics/
servers/**/plugins/FastAsyncWorldEdit/sessions/

# Chunky plugin task files
servers/**/plugins/Chunky/tasks/

# CMI plugin log files
servers/**/plugins/CMI/**Log**/

# CMILib plugin save and translation files
servers/**/plugins/CMILib/Saves/
servers/**/plugins/CMILib/Translations/Items/

# TAB plugin playerdata file
servers/**/plugins/TAB/playerdata.yml
servers/**/plugins/TAB/skincache.yml

# ------------------------------------------------------------------------------
# Security (Files that should never be committed for security reasons)
# ------------------------------------------------------------------------------
.assets/private/

# Ignore all private keys and certificates
*.pem
*.key
*.gpg

# If RCON passwords or similar are included, consider excluding server.properties as well
# server.properties

# ------------------------------------------------------------------------------
# Exceptions (Files that should be tracked exceptionally)
# ------------------------------------------------------------------------------
# Keep script execution permissions (depends on Git settings)
!*.sh

# Template files must be tracked
!*.template
!/templates/**

# Forcing tracking of specific configuration files
!server.properties
!bukkit.yml
!spigot.yml
!velocity.toml
!commands.yml
